title: IL POLIMATE
description: Intelligence Strategica.
author: marcorzzn
email: you@example.com

# --- 1. CONFIGURAZIONE CARTELLA (Fondamentale) ---
# Questo deve coincidere col nome della tua repository
baseurl: "/ilpolimate"

# Il dominio principale
url: "https://marcorzzn.github.io"

# --- 2. FORMATO LINK (La tua intuizione) ---
# Forza l'estensione .html per evitare errori 404 su GitHub Pages
permalink: /:year/:month/:day/:title.html

# --- 3. GESTIONE DATE ---
# Mostra i post anche se la data è "domani" per il server (evita 404 sui post nuovi)
future: true

markdown: kramdown

plugins:
  - jekyll-feed

exclude:
  - Gemfile
  - Gemfile.lock
  - daily_brief.py
  - README.md
  - requirements.txt
